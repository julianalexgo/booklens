import React from 'react';
import ReactDOM from 'react-dom/client';
import BookLens from './BookLens';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BookLens />
  </React.StrictMode>
);
